<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- xml파일을 사용할 mapper가 있는 경로 -->
<mapper namespace="com.lms.admin.curriculum.mapper.CurriculumMapper">
    <select id="getAllCurriculum" resultType="com.lms.admin.curriculum.dto.CurriculumDto">
        SELECT
            curriculum_id,
            curriculum_name,
            curriculum_content,
            curriculum_year,
            lecture_week,
            start_date,
            end_date,
            attendance_rate,
            curriculum_semester,
            curriculum_division,
            lecture_grade
        FROM curriculum
    </select>

</mapper>